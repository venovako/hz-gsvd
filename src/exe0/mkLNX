include ../mkLNX.inc

all: exe0.exe

exe0.exe: exe0.o ../HZ/libhz.a mkLNX ../mkLNX.inc
	$(FC) $(FCFLAGS) -mkl exe0.o -o$@ $(LDFLAGS)

exe0.o: exe0.F90 ../HZ/hz.mod mkLNX ../mkLNX.inc
	$(FC) $(FCFLAGS) -c exe0.F90

clean:
	-$(RM) exe0.o
	-$(RM) *.optrpt
	-$(RM) exe0.exe
